import { create, NButton, NInput, NMessageProvider, NModal, NCard, NDatePicker, NDataTable, NConfigProvider, NSelect, NEmpty } from "naive-ui";

const naive = create({
    components: [NButton, NInput, NMessageProvider, NModal, NCard, NDatePicker, NDataTable, NConfigProvider, NSelect, NEmpty ]
})

export default naive;