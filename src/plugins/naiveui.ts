import { create, NButton, NInput, NMessageProvider, NModal, NCard, NDatePicker, NDataTable, NConfigProvider, NSelect } from "naive-ui";

const naive = create({
    components: [NButton, NInput, NMessageProvider, NModal, NCard, NDatePicker, NDataTable, NConfigProvider, NSelect ]
})

export default naive;